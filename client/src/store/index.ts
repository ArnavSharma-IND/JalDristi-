/**
 * Global state store placeholder.
 *
 * For this hackathon scope, React's built-in useState + useEffect
 * in page components is sufficient. Add Zustand or Context here
 * if state sharing becomes complex.
 */
export {};
