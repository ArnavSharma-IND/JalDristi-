[project]
name = "jaldrishti-server"
version = "0.1.0"
description = "JalDrishti Backend - Real-time Groundwater Resource Evaluation"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"